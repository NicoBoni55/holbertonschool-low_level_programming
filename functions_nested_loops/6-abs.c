#include "main.h"
#include <ctype.h>
/**
 *_abs - checks for lowercase character.
 *@n: this variable is to return lowercase
 *
 * Return:0
 */
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
