#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns the length of a string
 * @s : return the lenght
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	while (s != NULL)
	{
	s++;
	}
	return (0);
}
