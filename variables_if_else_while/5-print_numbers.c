#include <stdio.h>
#include <stdlib.h>
/**
 *main - entry point
 *
 * Return: Always 0
 */
int main(void)
{
	int x;

	for (x = 48; x < 58; x++)
	if (x < 57)
	{
	putchar(x);
	}
	putchar('\n');
	return (0);
}
