#include <stdio.h>
/**
 * main - prints all single digit numbers
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char x;

	for (x = 48; x <= 57; x++)
	{
		putchar(x);
	}
	putchar('\n');

	return (0);
}
